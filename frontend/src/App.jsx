/* The code you provided is a commented-out section of a JavaScript React component. It appears to be
defining a component named `App` that renders a `DecisionSimulator` component within a `div`
element. However, since the code is commented out using `//`, it is not currently active and will
not be executed when the application runs. */
import DecisionSimulator from './DecisionSimulator';

function App() {
  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center">
      <DecisionSimulator />
    </div>
  );
}

export default App;

